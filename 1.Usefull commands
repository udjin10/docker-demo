##docker hub
docker login
docker pull nginx
docker push nginx
docker tag nginx my_nginx 


docker build --build-arg A=B .
docker history <image_name>

docker ps -a
docker image ls | docker images
docker image rm <name>

docker run -d --name nginx nginx 
docker container rm -f $(docker ps -a -q)

docker (container) exec -it nginx bash
Можно сразу: docker run -it --rm nginx bash
apt update && apt install -y htop psmisc

pstree -p | head 
docker run -d --name nginx nginx
docker exec -it nginx bash


docker restart nginx
docker stop nginx
docker rm -f nginx

docker logs -f !!!!
docker inspect <item> !!!!

docker volume ls 
docker network ls

sudo usermod -aG docker $USER && exec su -l $USER 

или reboot

или sudo 644 /var/run/docker.sock